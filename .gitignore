# Fehlerhaft erzeugter Ordner (PowerShell Glob-Expansion Artefakt)
{Config,GUI,Modules,Scripts}/

# Grosse Binaries (PDF liegt als SQLSetupTool.pdf im Root)
*.pdf

# LibreOffice Lock-Dateien
.~lock.*

# Temporäre Dateien
*.tmp
*.bak
Thumbs.db
desktop.ini

# C#/.NET build output (falls direkt hier gebaut wird)
bin/
obj/
*.user

# Quellcode lebt im privaten Repo SQLSetupToolSource, nicht hier
src/
*.sln
nuget.config
Archiv-*.zip
